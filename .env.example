# PostgreSQL
POSTGRES_USER=dbt
POSTGRES_PASSWORD=dbt_password
POSTGRES_DB=dbt_db
POSTGRES_SCHEMA=analytics

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin


# proxy
## if private ca certificates required, place them to ./docker/certs folder.

#HTTP_PROXY=
#HTTPS_PROXY=
#NO_PROXY=localhost

## for private cache repository, set the registry url. If not needed, leave it empty.
#PYTHON_IMAGE=
#POSTGRES_IMAGE=
#PGADMIN_IMAGE=
